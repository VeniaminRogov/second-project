{% extends 'base.html.twig' %}

{% block title %}Products page{% endblock %}

{% block content %}
    <h1 class="h1 text-center">
        Create products
    </h1>
    <div>
        {{ form(csvForm) }}
    </div>
    <div class="form">
        {{ form_start(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}